\clearpage
\appendix
\section*{Appendix}
\addcontentsline{toc}{section}{Appendix}

\section{Numerical validation against the reference implementation}
\label{app:validation}

Every stage of the sensory pipeline is checked against the original NumPy code
in double precision on random scenes (\texttt{scripts/validate\_physics.py}).
Agreement is at floating-point round-off, and the knollenorgan and ampullary
pathways agree bit-for-bit.

\input{validation_table}

\begin{figure}[h]
\centering
\includegraphics[width=0.62\textwidth]{../figs/fig1_validation.pdf}
\caption{Maximum relative deviation from the reference NumPy implementation over
random scenes, in double precision. Bars at the left edge are exactly zero.}
\label{fig:validation}
\end{figure}

\paragraph{An incidental bug in the reference code.} Its configuration file sets
a wall-reflection coefficient of $0.95$, but \texttt{ElectricScene.\_build\_%
reflections} never forwards it to \texttt{reflect\_sources}, whose default is
$1.0$. The published results therefore use unattenuated first-order images. We
match the code rather than the configuration file; using $0.95$ shifts the
corollary-discharge baseline by $\sim\num{1.4e-6}$ relative and changes ampullary
readings qualitatively, because those are a small difference of large numbers.

\section{Training the three channel conditions from scratch}
\label{app:scratch}

Training is bimodal: a seed either discovers patch foraging or stays near the
floor set by passive sensing alone. A seed counts as converged if it reaches half
the ceiling of its own task family, a criterion applied identically to every
condition within a family; the fraction clearing it is reported as its own
outcome, because conditions that remove sensory channels are not only worse on
average but less reliably learnable.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figs/fig2_channels_scratch.pdf}
\caption{Removing each consequence of a discharge, trained from scratch. Bars are
means over independent seeds with bootstrap 95\% CIs; points are seeds.
Percentages in (A) give the fraction of seeds reaching the learning criterion.}
\label{fig:scratch}
\end{figure}

\section{Regime classification}
\label{app:regimes}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figs/fig7_phase_grid.pdf}
\caption{Metabolic cost and eavesdropping predation jointly set discharge policy.
Rows are competitive and cooperative harvests; each cell is a mean over
independent seeds.}
\label{fig:phase}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.92\textwidth]{../figs/fig8_regimes.pdf}
\caption{Rule-based regime labels from the pooled metric vector. Thresholds are
percentiles of each metric's own distribution across the grid rather than
hand-set values, so the boundaries describe a continuum and are not claims about
sharp transitions.}
\label{fig:regimes}
\end{figure}

\section{The validation dyads in detail}
\label{app:assay}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figs/fig0_assay_validation.pdf}
\caption{Four dyads in the referential game. Only \emph{info+attend} is
communicating; the other three each satisfy some subset of the diagnostics.}
\label{fig:assay}
\end{figure}

\section{Why the learned referential game fails}
\label{app:optimiser}

\begin{figure}[h]
\centering
\includegraphics[width=0.72\textwidth]{../figs/fig0b_learned_referential.pdf}
\caption{A hand-coded dyad solves the referential game from the same
observations; twelve seeds each of three learned configurations, including one
with a published bias designed for this problem, do not leave chance.}
\label{fig:learnedref}
\end{figure}

The hand-coded receiver reads the subtype and the two site bearings out of the
observation vector and reaches the correct site on every trial, so the
information is present and a competent policy exists inside the representable
class. To check that the learned failure is not an artefact of exploration noise
or of the delay between committing to a direction and arriving, we re-ran the
receiver-only condition (scripted honest sender) with the initial policy standard
deviation on the steering action reduced from $e^{-0.5}$ to $e^{-2}$, and with the
two sites moved from \SI{60}{\centi\metre} apart at a range of
\SI{49}{\centi\metre} to \SI{40}{\centi\metre} apart at a range of
\SI{25}{\centi\metre}, which roughly halves the credit-assignment horizon and
doubles the number of trials per episode. Neither change moves performance off
chance:

\begin{center}\small
\begin{tabular}{lrr}
\toprule
Configuration & arrivals / episode & chance \\
\midrule
far sites, low steering noise & 2.01 of 4 & 2.0 \\
near sites, default noise & 3.00 of 6 & 3.0 \\
near sites, low steering noise & 3.07 of 6 & 3.0 \\
\bottomrule
\end{tabular}
\end{center}

We therefore attribute the failure to the difficulty of discovering a
multiplicative interaction between a single binary input and a continuous
steering policy under policy-gradient updates, rather than to the task geometry
or the exploration schedule.

\section{Generalisation of the decomposition}
\label{app:gen}
\input{gen_table}

\section{Message content and sender shaping}
\label{app:content}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figs/fig4_content.pdf}
\caption{What the pulse train carries. (A) Positive signalling, conditioned on
distance-to-neighbour decile. (B) Cross-validated decoding from receiver-observable
pulse counts only, minus the time-shifted baseline. (C) Discharge probability by
food proximity.}
\label{fig:content}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figs/fig6_ssi.pdf}
\caption{Sender shaping measured with reception held fixed. (A) SSI from the yoked
contrast, which cuts audibility only, beside the confounded deaf contrast, which
cuts reception. (B) Discharge rate, live versus yoked. (C) The free-riding effect
that drives the deaf contrast.}
\label{fig:ssi}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figs/fig5_listening.pdf}
\caption{Positive listening, and why it is not enough. (A) Interventional causal
influence against its noise floor (log scale). (B) Positive listening against a
rate-matched time-shift null and against silence, which is out of distribution.
(C) Phantom-pulse dose-response. (D) As metabolic cost rises, causal influence per
discharge increases while decodable content falls.}
\label{fig:listening}
\end{figure}

\section{The decoupled signalling variable}
\label{app:decoupled}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figs/fig9_decoupled.pdf}
\caption{A discharge subtype decoupled from sensing, crossed with competitive and
cooperative harvests and with a sparse-patch task in which finding food is the
bottleneck. (C) deletes the subtype while leaving the pulse, and therefore all
sensing, completely intact --- the manipulation the coupled channel does not
admit.}
\label{fig:decoupled}
\end{figure}

\section{The dual-use benchmark}
\label{app:dualuse}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figs/fig0c_dualuse.pdf}
\caption{A minimal dual-use foraging world with no fish physics: a \textsc{ping}
senses, illuminates neighbours and is registered as an event. The muting effect
decomposes the same way as in the electric-fish environment, and cost suppresses
the ping the same way.}
\label{fig:dualuse}
\end{figure}

\input{limitations}

\input{methods_body}
